import { Building2, Database, Fence, Layers3, MapPinned, Ruler, Route } from 'lucide-react';
import { useSketchStore } from '../store/useSketchStore';

export function MeasureRail() { const workspace=useSketchStore(s=>s.measureWorkspace),setWorkspace=useSketchStore(s=>s.setMeasureWorkspace),beginOffset=useSketchStore(s=>s.beginOffset);return <aside className="tool-rail measure-rail"><div className="rail-title"><Ruler size={19}/><span>Capture</span></div><div className="tool-list"><button className={`tool-button ${workspace==='structure'?'active':''}`} onClick={()=>setWorkspace('structure')}><Building2 size={21}/><span>Structures</span></button><button className={`tool-button ${workspace==='lot'?'active':''}`} onClick={()=>setWorkspace('lot')}><Route size={21}/><span>Lot lines</span></button><button className={`tool-button ${workspace==='road'?'active':''}`} onClick={()=>setWorkspace('road')}><MapPinned size={21}/><span>Roads</span></button><button className={`tool-button ${workspace==='offset'?'active':''}`} onClick={beginOffset}><Database size={21}/><span>Offsets</span></button><button className={`tool-button ${workspace==='fence'?'active':''}`} onClick={()=>setWorkspace('fence')}><Fence size={21}/><span>Fences</span></button><button className={`tool-button ${workspace==='surface'?'active':''}`} onClick={()=>setWorkspace('surface')}><Layers3 size={21}/><span>Surfaces</span></button></div></aside> }
